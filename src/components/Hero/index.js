import React from 'react';
import PropTypes from 'prop-types';

import './Hero.css';

const Hero = props => {
    return <div id="hero-container">Banner!</div>;
};

Hero.propTypes = {};

export default Hero;
